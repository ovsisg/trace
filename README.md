# Trace

**Trace** is a Python script that turns your webcam into a motion detector. It demonstrates using OpenCV for image processing, threading to keep the camera running smoothly while sending emails, and SMTP to send email alerts with images when motion is detected.
